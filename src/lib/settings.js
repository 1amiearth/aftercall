import { DEFAULT_MODEL } from './summary.js';

/** Settings in chrome.storage.local and their defaults. */
export const SETTINGS = { apiKey: '', model: DEFAULT_MODEL, selfName: '', mic: true, chat: true };
